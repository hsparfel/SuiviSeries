package com.pouillos.suiviseries.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.suiviseries.entities.ContactIgnore;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONTACT_IGNORE".
*/
public class ContactIgnoreDao extends AbstractDao<ContactIgnore, Long> {

    public static final String TABLENAME = "CONTACT_IGNORE";

    /**
     * Properties of entity ContactIgnore.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property IdPP = new Property(1, String.class, "idPP", false, "ID_PP");
        public final static Property FichierImport = new Property(2, String.class, "fichierImport", false, "FICHIER_IMPORT");
        public final static Property NumLigne = new Property(3, int.class, "numLigne", false, "NUM_LIGNE");
    }


    public ContactIgnoreDao(DaoConfig config) {
        super(config);
    }
    
    public ContactIgnoreDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CONTACT_IGNORE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"ID_PP\" TEXT NOT NULL ," + // 1: idPP
                "\"FICHIER_IMPORT\" TEXT," + // 2: fichierImport
                "\"NUM_LIGNE\" INTEGER NOT NULL );"); // 3: numLigne
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CONTACT_IGNORE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ContactIgnore entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getIdPP());
 
        String fichierImport = entity.getFichierImport();
        if (fichierImport != null) {
            stmt.bindString(3, fichierImport);
        }
        stmt.bindLong(4, entity.getNumLigne());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ContactIgnore entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getIdPP());
 
        String fichierImport = entity.getFichierImport();
        if (fichierImport != null) {
            stmt.bindString(3, fichierImport);
        }
        stmt.bindLong(4, entity.getNumLigne());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ContactIgnore readEntity(Cursor cursor, int offset) {
        ContactIgnore entity = new ContactIgnore( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // idPP
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // fichierImport
            cursor.getInt(offset + 3) // numLigne
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ContactIgnore entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setIdPP(cursor.getString(offset + 1));
        entity.setFichierImport(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setNumLigne(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ContactIgnore entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ContactIgnore entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ContactIgnore entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
