package com.pouillos.suiviseries.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.suiviseries.entities.Saison.LangageConverter;
import com.pouillos.suiviseries.entities.Serie;
import com.pouillos.suiviseries.enumeration.Langage;

import com.pouillos.suiviseries.entities.Saison;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SAISON".
*/
public class SaisonDao extends AbstractDao<Saison, Long> {

    public static final String TABLENAME = "SAISON";

    /**
     * Properties of entity Saison.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property SerieId = new Property(1, long.class, "serieId", false, "SERIE_ID");
        public final static Property NumSaison = new Property(2, int.class, "numSaison", false, "NUM_SAISON");
        public final static Property NbEpisodes = new Property(3, int.class, "nbEpisodes", false, "NB_EPISODES");
        public final static Property Langage = new Property(4, String.class, "langage", false, "LANGAGE");
        public final static Property Vu = new Property(5, boolean.class, "vu", false, "VU");
    }

    private DaoSession daoSession;

    private final LangageConverter langageConverter = new LangageConverter();

    public SaisonDao(DaoConfig config) {
        super(config);
    }
    
    public SaisonDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SAISON\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"SERIE_ID\" INTEGER NOT NULL ," + // 1: serieId
                "\"NUM_SAISON\" INTEGER NOT NULL ," + // 2: numSaison
                "\"NB_EPISODES\" INTEGER NOT NULL ," + // 3: nbEpisodes
                "\"LANGAGE\" TEXT," + // 4: langage
                "\"VU\" INTEGER NOT NULL );"); // 5: vu
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SAISON\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Saison entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSerieId());
        stmt.bindLong(3, entity.getNumSaison());
        stmt.bindLong(4, entity.getNbEpisodes());
 
        Langage langage = entity.getLangage();
        if (langage != null) {
            stmt.bindString(5, langageConverter.convertToDatabaseValue(langage));
        }
        stmt.bindLong(6, entity.getVu() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Saison entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSerieId());
        stmt.bindLong(3, entity.getNumSaison());
        stmt.bindLong(4, entity.getNbEpisodes());
 
        Langage langage = entity.getLangage();
        if (langage != null) {
            stmt.bindString(5, langageConverter.convertToDatabaseValue(langage));
        }
        stmt.bindLong(6, entity.getVu() ? 1L: 0L);
    }

    @Override
    protected final void attachEntity(Saison entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Saison readEntity(Cursor cursor, int offset) {
        Saison entity = new Saison( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // serieId
            cursor.getInt(offset + 2), // numSaison
            cursor.getInt(offset + 3), // nbEpisodes
            cursor.isNull(offset + 4) ? null : langageConverter.convertToEntityProperty(cursor.getString(offset + 4)), // langage
            cursor.getShort(offset + 5) != 0 // vu
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Saison entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSerieId(cursor.getLong(offset + 1));
        entity.setNumSaison(cursor.getInt(offset + 2));
        entity.setNbEpisodes(cursor.getInt(offset + 3));
        entity.setLangage(cursor.isNull(offset + 4) ? null : langageConverter.convertToEntityProperty(cursor.getString(offset + 4)));
        entity.setVu(cursor.getShort(offset + 5) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Saison entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Saison entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Saison entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getSerieDao().getAllColumns());
            builder.append(" FROM SAISON T");
            builder.append(" LEFT JOIN SERIE T0 ON T.\"SERIE_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Saison loadCurrentDeep(Cursor cursor, boolean lock) {
        Saison entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Serie serie = loadCurrentOther(daoSession.getSerieDao(), cursor, offset);
         if(serie != null) {
            entity.setSerie(serie);
        }

        return entity;    
    }

    public Saison loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Saison> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Saison> list = new ArrayList<Saison>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Saison> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Saison> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
